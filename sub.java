import java.util.*;
public class sub {
    public static void main(String[] args) {
        String[] s={"mahe","meh","mani"};
        Stack h=new Stack<>();
        Arrays.sort(s);
        int i,j=0;
        char[] m=s[0].toCharArray();
      // char[] n=s[s.length-1].toCharArray();
       System.out.println(m.length+" "+s[0].length());
      
    }
}
