import java.util.*;
public class yoyo {
    public static void main(String[] args) {
        HashSet m=new HashSet<>();
        m.add(5);
        m.add(10);
        m.add(10);
        System.out.println(m.size());
    }
}
